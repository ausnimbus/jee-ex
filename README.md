JEE Example
====================

This is an example for deploying JEE on AusNimbus or OpenShift

Note: to build this repository with maven you must specify "-Popenshift", eg "mvn clean package -Popenshift"
